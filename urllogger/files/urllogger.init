#!/bin/sh /etc/rc.common
# Copyright (C) 2006-2024 OpenWrt.org

START=98

start() {
	SERVICE_DAEMONIZE=1 service_start /usr/sbin/urllogger start
	echo "start ok"
}

stop() {
	if /usr/sbin/urllogger stop; then
		echo "stop ok"
	else
		echo "stop failed"
		return 1
	fi
}
